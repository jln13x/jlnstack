{
  "name": "root",
  "private": true,
  "engines": {
    "node": "^20",
    "pnpm": "^10.4.1"
  },
  "packageManager": "pnpm@10.4.1",
  "scripts": {
    "version": "changeset version",
    "cs": "changeset",
    "dev": "turbo run dev",
    "release": "pnpm build && changeset publish",
    "lint": "biome check",
    "format": "biome format --write",
    "build": "pnpm run build:procedure",
    "build:procedure": "pnpm --filter=@jlnstack/procedure build"
  },
  "devDependencies": {
    "@biomejs/biome": "2.2.0",
    "@changesets/cli": "^2.27.9",
    "size-limit": "^11.2.0",
    "turbo": "^2.5.8"
  }
}
